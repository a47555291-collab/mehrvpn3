[project]
name = "mehrvpn"
version = "0.1.0"
description = "Self-hosted OpenVPN administration with durable quota accounting"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
